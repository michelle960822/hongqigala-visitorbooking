# 预约核销系统 · 接口与数据表详细设计

> 前置结论（来自《预约系统设计方案》）：
> - 实名校验先落地 **本地身份证校验位**，权威二要素 API 作为可插拔模块后续接入。
> - 一人一天一时段 → `UNIQUE(id_number_hash, booking_date)`。
> - 时段限额防超卖 → `slot_capacity` 表 + 原子 `UPDATE ... WHERE available>0` + 事务。
> - 身份证属敏感个人信息：落库 AES 加密、展示脱敏、设留存期限。

---

## 1. 通用约定

- **Base URL**：`https://<host>/api`
- **鉴权**：微信小程序 `wx.login` 拿 `code` → 后端换 `openid` + 自建 `session_token`（HttpOnly cookie 或 Authorization 头）。游客无需注册，但需绑定 `openid` 防止同一人用多个微信重复预约（叠加身份证唯一索引双保险）。
- **请求/响应**：`Content-Type: application/json`
- **统一响应体**：
  ```json
  { "code": 0, "message": "ok", "data": { } }
  ```
  `code = 0` 成功；非 0 为业务错误（见第 6 节错误码）。
- **幂等**：写接口必须带 `Idempotency-Key` 请求头（前端生成 UUID），重复提交返回首次结果，不重复落库。

---

## 2. API 契约

### 2.1 查询某日期时段余量
`GET /v1/slots?date=2026-08-01`

响应 `data`：
```json
{
  "date": "2026-08-01",
  "slots": [
    { "slot_id": 1001, "start": "10:00", "end": "11:00", "capacity": 15, "available": 12 },
    { "slot_id": 1002, "start": "11:00", "end": "12:00", "capacity": 15, "available": 0 }
  ]
}
```
说明：`available=0` 前端直接置灰；余量实时读 `slot_capacity.available`（可加 5s 缓存，下单以 DB 为准）。

### 2.2 创建预约
`POST /v1/reservations`
Header：`Idempotency-Key: <uuid>`

请求体：
```json
{
  "slot_id": 1001,
  "booking_date": "2026-08-01",
  "name": "张三",
  "id_number": "11010119900307123X"
}
```

响应（成功 201）：
```json
{
  "code": 0,
  "data": {
    "reservation_id": "R20260801xxxx",
    "checkin_token": "ck_xxxxxxx",
    "qr_content": "https://<host>/c/ck_xxxxxxx",
    "status": "booked",
    "expire_at": "2026-08-01T11:00:00+08:00"
  }
}
```

### 2.3 查询我的预约
`GET /v1/reservations/:reservation_id` → 返回状态、时段、核销码（用于页面展示）。

### 2.4 核销
`POST /v1/checkin`
```json
{ "checkin_token": "ck_xxxxxxx" }
```
响应：`{ "code": 0, "data": { "status": "checked_in", "checked_at": "..." } }`
幂等：已核销再次请求返回 `code=20006` 并带原 `checked_at`，不报错。

### 2.5 取消预约（可选）
`POST /v1/reservations/:id/cancel` → 回补 `available`（原子 `+1`），状态置 `cancelled`。

### 2.6 管理后台（内部）
- `GET /v1/admin/slots?date=` 各时段余量 + 已约/已核销数
- `GET /v1/admin/reservations?slot_id=&status=` 名单（身份证脱敏）
- `POST /v1/admin/checkin` 工作人员手动核销

---

## 3. 身份证本地校验算法（服务端必做）

仅校验格式与第 18 位校验码，**不能**确认人证一致（那步留给后续二要素 API）。

**步骤**：
1. 长度 18，前 17 位为数字，末位为数字或 `X`。
2. 地区码（前 6 位）在合法行政区划表内（可选，防明显乱填）。
3. 出生日期（7–14 位）为合法日期。
4. **校验位**：权重 `wi = [7,9,10,5,8,4,2,1,6,3,7,9,10,5,8,4,2]`；
   `s = Σ(id[i] × wi[i])`，`i = 0..16`；
   `c = s % 11`；校验码映射 `['1','0','X','9','8','7','6','5','4','3','2'][c]`；
   与第 18 位比较（不区分大小写）。

**伪代码**：
```python
def valid_id(idn: str) -> bool:
    if len(idn) != 18: return False
    if not idn[:17].isdigit(): return False
    if idn[17] not in "0123456789Xx": return False
    wi = [7,9,10,5,8,4,2,1,6,3,7,9,10,5,8,4,2]
    codes = "10X98765432"
    s = sum(int(idn[i]) * wi[i] for i in range(17))
    return codes[s % 11] == idn[17].upper()
```
> 姓名做基础格式校验（2–20 字、不含数字/特殊符号），不在此做真人核验。

---

## 4. 数据表结构（详细）

### 4.1 slot_capacity（时段配额）
| 字段 | 类型 | 说明 |
|------|------|------|
| slot_id | BIGINT PK | 时段 ID |
| activity_date | DATE | 活动日期 |
| start_time | TIME | 开始 |
| end_time | TIME | 结束 |
| capacity | INT | 总名额（如 15） |
| available | INT | 剩余名额（原子扣减对象） |
| version | INT | 乐观锁（可选，与 UPDATE 条件二选一） |

### 4.2 reservations（预约记录）
| 字段 | 类型 | 说明 |
|------|------|------|
| reservation_id | VARCHAR(32) PK | 业务 ID |
| openid | VARCHAR(64) | 微信标识（防多微信刷） |
| id_number_hash | CHAR(64) | 身份证**加盐 SHA256**（建唯一索引用，不存明文） |
| id_number_enc | VARBINARY(255) | 身份证 AES 加密（确需回看时解密） |
| name_enc | VARBINARY(255) | 姓名 AES 加密 |
| slot_id | BIGINT | 时段 |
| booking_date | DATE | 预约日期 |
| status | TINYINT | 0 待核销 1 已核销 2 已取消 |
| checkin_token | VARCHAR(64) UNIQUE | 核销码（签名 token） |
| idempotency_key | VARCHAR(64) UNIQUE | 幂等键 |
| created_at | DATETIME | 创建时间 |
| checked_at | DATETIME NULL | 核销时间 |

### 4.3 checkin_log（核销流水，可选）
`log_id, reservation_id, operator, channel, created_at` —— 用于审计与对账。

---

## 5. 索引设计

```sql
-- reservations
UNIQUE KEY uk_id_date (id_number_hash, booking_date);  -- 一人一天一时段
UNIQUE KEY uk_token (checkin_token);
UNIQUE KEY uk_idem (idempotency_key);
KEY idx_slot (slot_id);
KEY idx_status (status);

-- slot_capacity
UNIQUE KEY uk_date_slot (activity_date, start_time);   -- 防止重复生成时段
```

> 用 `id_number_hash` 而非明文建唯一索引：既满足去重，又不把明文暴露给索引页。

---

## 6. 错误码

| code | 含义 | HTTP |
|------|------|------|
| 0 | 成功 | 200/201 |
| 20001 | 身份证格式/校验位错误 | 400 |
| 20002 | 时段不存在或未开放 | 400 |
| 20003 | 名额已满（售罄） | 409 |
| 20004 | 该身份证当日已预约 | 409 |
| 20005 | 核销码无效 | 400 |
| 20006 | 已核销（幂等返回） | 200 |
| 20007 | 重复提交（幂等键已存在） | 200 |
| 40001 | 未登录/会话失效 | 401 |
| 50000 | 系统错误 | 500 |

---

## 7. 创建预约事务伪代码（防超卖核心）

```sql
START TRANSACTION;
-- 1) 先抢名额（行锁串行化，available>0 保证不超卖）
UPDATE slot_capacity
   SET available = available - 1
 WHERE slot_id = ? AND available > 0;
IF ROW_COUNT() = 0 THEN
   ROLLBACK; RETURN 20003;          -- 售罄
END IF;
-- 2) 再插入预约（唯一索引兜底：一人一天 + 幂等键）
INSERT INTO reservations
   (reservation_id, openid, id_number_hash, id_number_enc, name_enc,
    slot_id, booking_date, status, checkin_token, idempotency_key, created_at)
VALUES (?, ?, ?, ?, ?, ?, ?, 0, ?, ?, NOW());
-- 唯一冲突 -> 20004（身份证当日已约）或 20007（幂等键重复）
-- 捕获 DuplicateKey:
--   ROLLBACK; RETURN (20004 或 20007);
COMMIT;
RETURN 201 + checkin_token;
```
**要点**：先扣减再插入；两者在同一事务，任一失败整体回滚，名额不会被"占坑不写"。并发靠 `slot_capacity` 行锁天然隔离，无需 Redis 锁。

---

## 8. 核销与幂等

```sql
UPDATE reservations
   SET status = 1, checked_at = NOW()
 WHERE checkin_token = ? AND status = 0;
-- ROW_COUNT()=1 -> 成功核销
-- ROW_COUNT()=0 且原 status=1 -> 返回 20006（已核销，幂等）
-- ROW_COUNT()=0 且查无 -> 20005（无效码）
```
防截图复用：核销码单次有效 + 现场核对身份证末 4 位 / 人脸；可加短时效 `expire_at`。

---

## 9. 限流 / 防刷 / 合规

- **防刷**：图形/短信验证码（热门时段开启）；接口限流（同 `openid`/IP 每秒 N 次）；`Idempotency-Key` 防双击。
- **合规（PIPL）**：
  - 明示《个人信息授权书》，用户勾选后才采集身份证。
  - 落库 AES-256 加密（`id_number_enc`/`name_enc`），密钥 KMS 托管；建索引用加盐哈希。
  - 展示脱敏：`110***********1234`。
  - 留存期限：活动结束后 N 天自动脱敏/删除。
- **压测**：热门时段模拟并发抢同一 slot，验证 `available` 不为负、不超卖。
