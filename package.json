{
  "name": "auto-expo-booking",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "汽车体验活动预约核销系统（Cloudflare Workers + D1 / 腾讯云 CloudBase 通用）",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/smoke.mjs"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
