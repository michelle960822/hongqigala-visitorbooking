# 汽车体验活动 · 预约核销系统 设计方案

> 适用场景：线下汽车体验活动，对自然游客开放，需提前预约日期/时段，到场核销体验。
> 三个硬需求：① 实名校验（姓名+身份证）② 一人一天一时段 ③ 每个时段限额（如每小时 max 15 人）。

---

## 一、三个硬需求的本质

| 需求 | 本质 | 解法核心 |
|------|------|----------|
| 实名校验 | 格式正确 ≠ 真人实名，分"本地校验"和"权威核验"两层 | 身份证校验位 + （可选）二要素 API |
| 一人一天一时段 | 去重 + 幂等 | 唯一索引 `UNIQUE(id_number, date)` |
| 时段限额 | 高并发库存扣减，防超卖 | 原子 `UPDATE ... WHERE available>0` + 事务 |

---

## 二、推荐整体架构

- **入口**：微信小程序（最顺——扫码即用、自带登录、核销扫码方便）或 H5 嵌入公众号。自然游客扫活动码进入。
- **后端**：预约服务 + 核销服务，无状态，可水平扩展。
- **存储**：MySQL（主） + Redis（限流/缓存可选）。时段配额靠 DB 行锁/原子 UPDATE 即可，不必上 Redis 分布式锁。
- **通信**：小程序 → HTTPS API，全程 TLS。

---

## 三、实名校验怎么落地

**第一层（必须，免费）— 服务端本地校验：**
- 18 位格式、地区码、出生日期合法性。
- 校验位算法（ISO 7064 MOD 11-2）：前 17 位加权求和 `% 11`，按映射表得到第 18 位校验码，不符直接拒绝。
- 姓名基础格式校验（长度、字符范围）。

**第二层（可选，按合规要求）— 权威实名核验 API：**
- 公安 / 运营商 / 支付宝 / 微信 的"姓名+身份证二要素"核验，确认人证一致。
- 付费接口，需合规授权：必须让用户勾选《个人信息授权书》后再调用。

**合规（PIPL 个人信息保护法）：**
- 明示告知用途、最小必要采集、加密存储（AES 字段加密，密钥 KMS 托管）、展示脱敏（如 `110***********1234`）、设定留存期限（活动结束后 X 天删除）。

---

## 四、一人一天一时段

- 预约表加唯一索引 `UNIQUE(id_number, booking_date)`。
- 下单先尝试插入；唯一冲突即返回"该身份证当天已预约"。
- 前端再叠加防重复提交（按钮禁用 + 请求幂等 token），但唯一索引是最后兜底。

---

## 五、时段限额（防超卖）— 重点

最稳的方案：一张 `slot_capacity` 表，每行一个时段，字段 `available`（剩余名额）。下单时在**一个事务**里：

1. `UPDATE slot_capacity SET available = available - 1 WHERE slot_id = ? AND available > 0;`
2. 若 `affected_rows = 0` → 已售罄，回滚并返回"名额已满"。
3. 插入预约记录（唯一索引兜底一人一天）。若唯一冲突 → 回滚扣减，返回"已预约"。
4. 提交事务。

靠 DB 行锁天然串行化，"单人单时段"与"名额上限"同时保证，**不需要分布式锁**。
取消预约：`available + 1`（同样原子），或软删除后回补名额。

---

## 六、核销流程

- 下单成功生成**一次性核销码/二维码**（含签名 token，绑定预约 ID，可设时效）。
- 到场由工作人员扫码（或用户出示，工作人员确认）：
  `UPDATE reservation SET status='checked_in' WHERE id=? AND status='booked';`
- 幂等：已核销不重复处理；防截图复用——单次有效 + 现场核对身份证末 4 位/人脸。

---

## 七、别忘了的工程细节

- **防刷**：图形/短信验证码、接口限流（同 IP/设备频率）。
- **兜底**：活动前短信/模板消息提醒；迟到未到可放号给候补。
- **管理后台**：查看各时段余量、现场核销、导出实名名单（脱敏）。
- **压测**：热门时段开抢，验证不超卖。

---

## 八、最小可用 MVP 建议

先上：小程序/H5 + MySQL + 身份证校验位 + 唯一索引 + 原子扣减 + 二维码核销。
权威实名核验作为**可插拔模块**，等合规确认后再接入。

---

## 附：数据表结构示例（伪 SQL）

```sql
-- 时段配额表（每行一个时段）
CREATE TABLE slot_capacity (
  slot_id       BIGINT PRIMARY KEY,
  activity_date DATE    NOT NULL,
  start_time    TIME    NOT NULL,
  end_time      TIME    NOT NULL,
  capacity      INT     NOT NULL,   -- 例如 15
  available     INT     NOT NULL    -- 剩余名额，原子扣减
);

-- 预约记录表
CREATE TABLE reservations (
  id             BIGINT PRIMARY KEY,
  id_number_enc  VARBINARY(255) NOT NULL,  -- 身份证 AES 加密
  name_enc       VARBINARY(255) NOT NULL,  -- 姓名 AES 加密
  slot_id        BIGINT NOT NULL,
  booking_date   DATE   NOT NULL,
  status         TINYINT NOT NULL DEFAULT 0, -- 0 待核销 1 已核销 2 已取消
  checkin_token  VARCHAR(64) NOT NULL,
  created_at     DATETIME NOT NULL,
  UNIQUE KEY uk_id_date (id_number_hash, booking_date)  -- 一人一天
);
```

> 说明：`id_number` 直接存明文有合规风险，建议落库加密（ enc 字段），唯一索引可用身份证的**加盐哈希**建，而不是明文。
